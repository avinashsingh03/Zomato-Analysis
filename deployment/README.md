# Zomato Restaurant Rating Predictor

A Streamlit app to predict restaurant ratings based on customer reviews and metadata.

## How to run locally

```bash
pip install -r requirements.txt
streamlit run app.py
